class MinStack {

    Stack<Integer> stack, aux;

    public MinStack() {
        stack = new Stack<>();
        aux = new Stack<>();
    }
    
    public void push(int val) {
        stack.push(val);
        if(aux.isEmpty() || val <= aux.peek()){
            aux.push(val);
        }
    }
    
    public void pop() {
        if(stack.pop().equals(aux.peek()))
            aux.pop();
    }
    
    public int top() {
        return stack.peek();
    }
    
    public int getMin() {
        return aux.isEmpty() ? -1 : aux.peek();
    }
}

/**
 * Your MinStack object will be instantiated and called as such:
 * MinStack obj = new MinStack();
 * obj.push(val);
 * obj.pop();
 * int param_3 = obj.top();
 * int param_4 = obj.getMin();
 */


Time : O(1) for each operation
Space : O(n) 
